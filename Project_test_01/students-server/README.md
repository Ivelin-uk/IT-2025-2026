# 🎓 Students Management API

Пълноценен REST API сървър за управление на студенти с web интерфейс.

## 🚀 Стартиране

### 1. Инсталиране на зависимости
```bash
cd students-server
npm install
```

### 2. Стартиране на сървъра
```bash
npm start
```

### 3. Отваряне в браузъра
Отидете на: http://localhost:3000

## 📊 API Endpoints

### Студенти
- `GET /api/students` - Всички студенти
- `GET /api/students/:id` - Конкретен студент
- `POST /api/students` - Добави студент
- `PUT /api/students/:id` - Редактирай студент
- `DELETE /api/students/:id` - Изтрий студент

### Търсене и статистики
- `GET /api/students/search/:query` - Търсене на студенти
- `GET /api/stats` - Статистики

## 🧪 Тестване с curl

### Вземи всички студенти
```bash
curl http://localhost:3000/api/students
```

### Добави нов студент
```bash
curl -X POST http://localhost:3000/api/students \
  -H "Content-Type: application/json" \
  -d '{
    "firstName": "Анна",
    "lastName": "Стоянова", 
    "email": "anna.stoyanova@uktc.edu",
    "age": 19,
    "course": "IT",
    "year": 1,
    "gpa": 5.9
  }'
```

### Редактирай студент
```bash
curl -X PUT http://localhost:3000/api/students/1 \
  -H "Content-Type: application/json" \
  -d '{
    "firstName": "Иван",
    "lastName": "Петров",
    "email": "ivan.petrov@uktc.edu", 
    "age": 21,
    "course": "IT",
    "year": 3,
    "gpa": 6.0
  }'
```

### Изтрий студент
```bash
curl -X DELETE http://localhost:3000/api/students/1
```

### Търсене
```bash
curl http://localhost:3000/api/students/search/иван
```

### Статистики
```bash
curl http://localhost:3000/api/stats
```

## 🎯 Функционалности

### Web интерфейс
- **Преглед на студенти** - Grid layout с карточки
- **Добавяне на студенти** - Форма с валидация
- **Редактиране** - Click за редакция
- **Търсене** - Реално време
- **Статистики** - Визуални карточки

### API възможности
- **CRUD операции** (Create, Read, Update, Delete)
- **Търсене** по име, имейл, курс
- **Статистики** - средни стойности и разпределения
- **Валидация** на данни
- **Error handling**
- **CORS поддръжка**

## 📁 Структура

```
students-server/
├── server.js          # Основен сървър файл
├── package.json       # NPM конфигурация
├── public/            # Статични файлове
│   └── index.html     # Web интерфейс
└── README.md          # Документация
```

## 🛠️ Технологии

- **Backend:** Node.js + Express
- **Frontend:** Vanilla HTML/CSS/JavaScript
- **База данни:** In-memory (за демо)
- **Стилове:** CSS Grid & Flexbox

## 📈 Примерни данни

Сървърът включва 3 примерни студента:
- Иван Петров (IT, 2 курс)
- Мария Иванова (Дизайн, 1 курс) 
- Георги Димитров (IT, 3 курс)

## 🔧 Възможни подобрения

- MongoDB/MySQL интеграция
- JWT автентикация
- Файл upload за снимки
- Export/Import от Excel
- Email нотификации
- Pagination за много данни

## 📝 Лиценз

MIT License